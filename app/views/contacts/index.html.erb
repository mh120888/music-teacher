<h1>Your Contacts:</h1>
<ul>
  <% @contacts.each do |contact| %>
    <li><%= contact.first_name %> <%= contact.last_name %></li>
  <% end %>
</ul>

<%= link_to 'Add New Contact', new_contact_path, :remote => true %>