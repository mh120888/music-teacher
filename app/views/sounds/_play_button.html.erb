    <%= form_tag(sc_play_path, class: "play play-form", :method => 'post', :remote => true, :id => track.id) do %>
      <%= image_tag track.artwork_url %><br>
      <%= hidden_field_tag 'track_url', track.permalink_url %>
      <%= button_to 'Play!' , :disabled => true %>
    <% end %>
